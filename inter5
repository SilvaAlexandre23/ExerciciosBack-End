function numerosPares(arr) {
    let pares = [];

    for (let num of arr) {
        if (num % 2 === 0) {
            pares.push(num);
        }
    }

    return pares;
}
console.log(numerosPares([2,3,4,5,6,7,8]))
